<!-- PR_BODY_FILE: runs/20260526-100829653_feat-niid-visual-table-review-v1/PR.md -->

## まとめ

NIID付表・別表の視覚レビュー復元を開始し、元PDFページ画像をRUN成果物に含めました。まず `付表3` と `付表4` をPDF画像から読み直し、テキスト抽出だけでは扱えなかった結合ヘッダ、親項目、複数行セルを復元しています。

## 変更内容

- NIID対象ページのPDFレンダリング画像を `source_pages/` に追加
- `付表3` のBSL表を視覚復元
- `付表4` のABSL表を視覚復元
- 復元結果をJSON/Markdownで記録
- 次対象として `付表2`, `別表7`, `別表10` の画像も同梱

## 検証

ドキュメント・画像成果物のみの変更。コード・テストは変更していません。

## 次アクション

次PRでは、同梱済み画像を使って `付表2`, `別表7`, `別表10` の視覚復元を進めます。
