# PROMOTION_CANDIDATE_REVIEW

## 結論

今回のユーザー指定はPhase 0-6までのため、review_candidate / promotion_candidate は作成していない。`data/normalized/` へのコピーも行っていない。

## 次の候補

1. EU GMP Vol.4 Chapter 1
2. PIC/S PE 009-17 Annex 15
3. PIC/S Annex 11

## 作成時の条件

- `runs/<run_id>/review_candidate/<doc_id>/` に4ファイル、manifest、GOAL_CHECK_RESULT、SAMPLE_COMPARISON、QUALITYCHECK_RESULTを置く。
- `data/normalized/` へは承認前にコピーしない。
