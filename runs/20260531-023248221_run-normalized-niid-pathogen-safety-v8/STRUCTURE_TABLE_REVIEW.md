# 構造・表レビュー

## 対象

- 文書: `jp_niid_pathogen_safety_management_20240401`
- 原文: `data/human-readable/niid/pathogen_safety_management/source_texts/Kanrikitei3_20240401.txt`
- 出力: `promotion_candidate/`

## 目検方針

- Headingは条・別表単位で原文見出しと照合した。
- 表4/表5は、PDF/TXT上の見た目だけではなく、Markdown表として再結合したときの列数とセル位置を確認した。
- Markdownでは結合セルを表現できないため、結合セルの値は対象列すべてに複製した。

## 表4

- `対象病原体等BSL` から `実験室まで通行制限` までは大項目のみの行として扱い、小項目は `－` とした。
- `保管施設（庫）` 以降は大項目・小項目形式として扱った。
- `実験室` と `実験室内` の結合セル行は、全BSL列へ `実験室` を複製した。
- `感染動物の飼育設備` と `滅菌設備` は大項目のみの行として扱い、小項目は `－` とした。
- `維持管理` は大項目・小項目形式として扱った。

## 表5

- 表として扱う範囲は `対象病原体等BSL` から `排水の滅菌等` まで。
- `保管の基準`、`使用の基準`、`滅菌等の基準` は全て大項目・小項目形式として扱った。
- `○ 運搬の基準（1種～4種病原体等）` 以降は表外の箇条書きであり、表5のtable_rowには含めない。
- 運搬基準本文は `ann5.not3` のnoteとして保持されている。

## 再結合チェック

- `TABLE4_5_RECONSTRUCTION_CHECK.md` にIRから再構成したMarkdown表を収録。
- 別表4: 9列、38行、列数整合PASS。
- 別表5: 9列、17行、列数整合PASS。
