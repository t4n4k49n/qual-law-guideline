<!-- PR_BODY_FILE: runs/20260526-112428711_feat-niid-visual-table-review-v2/PR.md -->

## まとめ

NIID付表・別表の視覚レビュー復元を進め、未復元だった `付表2`, `別表7`, `別表10` を表recordとして整理しました。これにより、前回の `付表3`, `付表4` と合わせて、PDFテキストレイヤーだけでは不安定だった視覚復元対象を一通りレビュー済みの構造データとして参照できる状態にしました。

## 変更内容

- `付表2`, `別表7`, `別表10` の元PDFページ画像をRUN成果物に同梱
- `付表2` の4 recordを視覚復元
- `別表7` の18 recordを視覚復元
- `別表10` の13 recordを視覚復元
- 左端結合セルを `category` として展開
- 復元結果をJSON/Markdownで記録

## 検証

- `visual_reconstruction.json` のJSON妥当性確認
- `git diff --check`

コード・テストは変更していません。

## 次アクション

正式な正規化RUNへ進む場合は、v1/v2の視覚復元JSONを昇格候補の入力として扱い、既存parser/adapter出力との突合を行います。
